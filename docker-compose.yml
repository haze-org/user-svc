name: apricity-user-svc

services:
  postgresDB:
    image: postgres:16
    container_name: user-svc-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: user
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
        - ./postgres_data:/var/lib/postgresql/data